[package]
name = "ferdirust"
version = "0.1.0"
edition = "2024"

[dependencies]
cef = { version = "145.1.1", default-features = false, features = ["build-util"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
dirs = "6"
